[pytest]
# Directorio raíz para buscar tests
testpaths = tests

# Mostrar salida detallada por defecto
addopts = -v --tb=short

# Agrega el backend/ al Python path para que los imports funcionen
# (ej: "from core.security import ..." sin necesidad de instalar el paquete)
pythonpath = backend
